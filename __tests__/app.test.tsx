import { expect, test } from "vitest";
import { render } from "@testing-library/react";
import App from "./../src/App";

test("basic test to see of counting works", async () => {
    render(<App />;)
	
});
